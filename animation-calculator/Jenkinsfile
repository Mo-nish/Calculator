pipeline {
    agent any

    environment {
        SONARQUBE = 'Calculator'
        DOCKER_IMAGE = 'localhost:5000/calculator-app:latest'
        APP_PATH = 'backend'
    }

    stages {

        stage('Checkout') {
            steps {
                echo '📥 Checking out code...'
                git branch: 'main', credentialsId: 'github-pat', url: 'https://github.com/Mo-nish/Calculator.git'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('Calculator') {
                    bat """
                    sonar-scanner ^
                        -Dsonar.projectKey=Calculator ^
                        -Dsonar.sources=. ^
                        -Dsonar.host.url=http://localhost:9000 ^
                        -Dsonar.token=%SONAR_TOKEN%
                    """
                }
            }
        }

        stage('Quality Gate') {
            steps {
                timeout(time: 10, unit: 'MINUTES') {
                    script {
                        def qg = waitForQualityGate()
                        if (qg.status != 'OK') {
                            error "🚫 Quality Gate failed: ${qg.status}"
                        }
                    }
                }
            }
        }

        stage('Run Unit Tests') {
            steps {
                echo '🧪 Running Python unit tests...'
                bat """
                    cd %APP_PATH%
                    pytest --maxfail=1 --disable-warnings -q
                """
            }
        }

        stage('Build Web Docker Image') {
            steps {
                echo '🐳 Building Flask Docker image...'
                bat """
                    docker build -t %DOCKER_IMAGE% %APP_PATH%
                """
            }
        }

        stage('Verify Docker Image') {
            steps {
                bat """
                    echo === Verifying built image ===
                    docker images %DOCKER_IMAGE%
                """
            }
        }

        stage('Build Desktop Executable') {
            steps {
                echo '💻 Packaging desktop version...'
                bat """
                    cd %APP_PATH%
                    pip install pyinstaller
                    pyinstaller --onefile --noconsole desktop_app.py --name AnimationCalculator
                """
            }
        }

        stage('Archive Desktop Executable') {
            steps {
                echo '📦 Archiving .exe artifact...'
                archiveArtifacts artifacts: '%APP_PATH%/dist/AnimationCalculator.exe', fingerprint: true
            }
        }

    }

    post {
        success {
            echo '✅ All builds completed successfully — Web + Desktop versions ready!'
        }
        failure {
            echo '❌ Build failed. Check stage logs.'
        }
    }
}
